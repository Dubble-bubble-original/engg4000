// React
import { Container } from 'react-bootstrap';

function Create() {

  return (
    <Container className="outer-container" testid="create-page">
      {/* Temporary content (change me) */}
      <div testid="create-title" className="h4 mb-0">Follow these steps to create a new post</div>
    </Container>
  )
}

export default Create;